import React from "react";

export default function StickyCTA(){
    return (
        <div className="sticky-cta">
            <a href="#pricing">Get started — Book a demo</a>
        </div>
    );
}